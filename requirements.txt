# ============================================================
# Core Web Framework
# ============================================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0

# ============================================================
# Data Validation & Settings Management
# ============================================================
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.7
email-validator>=2.1.0
PyYAML>=6.0        # config/policy.yaml loader (app/core/policy.py)

# ============================================================
# Database (PostgreSQL + SQLAlchemy + Alembic Migrations)
# ============================================================
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.1

# ============================================================
# Redis (For LangGraph Checkpointing and Caching)
# ============================================================
redis>=5.0.1

# ============================================================
# AI / LLM / Agent Stack
# ============================================================
langchain>=0.1.0
langchain-core>=0.1.10
langgraph>=0.0.21
# Checkpointer backends (see app/core/memory.py): Redis Stack preferred, Postgres
# durable fallback, in-memory last resort. Redis needs Search+JSON modules
# (Redis Stack, not plain redis:7-alpine). Postgres needs psycopg v3 + its pool.
langgraph-checkpoint-redis>=0.0.1
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.1.0
langchain-openai>=0.0.2
openai>=1.0.0     # OpenAI-compatible client used by app/agents/llm_scorer.py (OpenRouter)

# ============================================================
# Authentication (JWT)
# ============================================================
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ============================================================
# Resilience (Retries + Circuit Breaker)
# ============================================================
tenacity>=8.2.3
pybreaker>=1.0.1

# ============================================================
# Scheduling (cron/interval dispatcher for scheduled emails)
# ============================================================
apscheduler>=3.10,<4

# ============================================================
# Bulk lead import (Excel .xlsx parsing; CSV/JSON use the stdlib)
# ============================================================
openpyxl>=3.1

# ============================================================
# Logging (Structured JSON)
# ============================================================
loguru>=0.7.2

# ============================================================
# HTTP Client (for tool API calls)
# ============================================================
requests>=2.31.0
httpx>=0.26.0

# ============================================================
# Testing
# ============================================================
pytest>=7.4.4
pytest-cov>=4.1.0
pytest-asyncio>=0.23.4
httpx>=0.26.0
